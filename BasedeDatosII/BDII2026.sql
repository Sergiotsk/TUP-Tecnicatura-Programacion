CREATE TABLE auditoria
( id_auditoria NUMBER(6,0)
GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, 
usuario VARCHAR2(30 BYTE)NOT NULL, fecha 
DATE DEFAULT sysdate NOT NULL, motivo VARCHAR2(255 BYTE) NOT NULL );


